{
  "apiKey": "YOUR_GEMINI_API_KEY", // Replace with your actual Gemini API key
  "apiUrl": "https://generativelanguage.googleapis.com/v1beta", // Base URL for the Gemini API (v1beta)
  "model": "models/gemini-pro", // Specifies the model to use (e.g., gemini-pro, gemini-1.5-pro-latest)
  "method": "generateContent" // The API method to call (e.g., generateContent, streamGenerateContent, embedContent)
}
